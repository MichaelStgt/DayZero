<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DayZero.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:desktop="clr-namespace:DayZero.View.Desktop"
    xmlns:tablet="clr-namespace:DayZero.View.Tablet"
    xmlns:view="clr-namespace:DayZero.View"
    Shell.FlyoutBehavior="Flyout">
    <!--  Title="{x:Binding Title}"  -->

    <ShellContent
        Title="Dashboard"
        ContentTemplate="{DataTemplate DashboardView}"
        Icon="dashboard.png"
        Route="DashboardView" />

    <ShellContent
        Title="{x:Static AppResources.Onboarding}"
        ContentTemplate="{x:DataTemplate OnboardingSubstanceView}"
        Icon="{x:StaticResource OnboardingIcon}" />
    <!--<ShellContent
        Title="{x:Static AppResources.Dashboard}"
        ContentTemplate="{x:DataTemplate DashboardView}"
        Icon="dashboard.png" />-->

    <!--<ShellContent
        Title="{x:Static AppResources.Home}"
        ContentTemplate="{x:OnIdiom Desktop={x:DataTemplate desktop:MainView},
                                    Tablet={x:DataTemplate tablet:MainView},
                                    Default={x:DataTemplate MainView}}"
        Icon="{x:StaticResource HomeIcon}" />-->

    <!--<ShellContent
        Title="{x:Static AppResources.About}"
        ContentTemplate="{x:DataTemplate AboutView}"
        Icon="{x:StaticResource AboutIcon}" />-->


</Shell>
