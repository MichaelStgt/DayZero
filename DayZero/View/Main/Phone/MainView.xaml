<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="DayZero.View.MainView"
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ContentPage.Behaviors>
        <StatusBarBehavior
            StatusBarColor="{x:AppThemeBinding Light={x:StaticResource BackgroundColorLight},
                                               Dark={x:StaticResource BackgroundColorDark}}"
            StatusBarStyle="{x:AppThemeBinding Light=LightContent,
                                               Dark=DarkContent}" />
        <!--  LightContent, DarkContent  -->
    </ContentPage.Behaviors>
    <Grid
        ColumnDefinitions="1*"
        RowDefinitions="Auto, 1*">
        <PagePlatformHeader
            Margin="{x:StaticResource DefaultElementMargin}"
            FontSize="{x:StaticResource SmallFontSize}"
            InfoName="Page:"
            IsVisible="{x:DynamicResource DisplayPageHeader}"
            SymbolFontSize="14"
            TextColor="{x:AppThemeBinding Light={x:StaticResource OffBlack},
                                          Dark={x:StaticResource OffWhite}}"
            Value="Phone.MainView" />

        <VerticalStackLayout
            Grid.Row="1"
            HorizontalOptions="Center"
            Spacing="25"
            VerticalOptions="Center">


            <Label
                SemanticProperties.HeadingLevel="Level1"
                Style="{StaticResource Headline}"
                Text="Hello, World!" />

            <Label
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                SemanticProperties.HeadingLevel="Level2"
                Style="{StaticResource SubHeadline}"
                Text="Welcome to &#10;.NET Multi-platform App UI" />

            <Button
                x:Name="CounterBtn"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill"
                SemanticProperties.Hint="Counts the number of times you click"
                Text="Click me" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
